JRTPLIB_BUILD_DIR = build

all:
	# @rm -f myRtspClientConfig/cmake_config_build.armlinux 
	@mkdir -p $(JRTPLIB_BUILD_DIR)
	@echo "Compile jrtplib"
	# (cd myRtspClientConfig/; perl config_armlinux.pl ../../../config.armlinux)
	# @cat myRtspClientConfig/cmake_config_build.armlinux CMakeLists.txt_backup > CMakeLists.txt
	(cd $(JRTPLIB_BUILD_DIR); rm -f Makefile; cmake ..; make)

clean:
	(cd $(JRTPLIB_BUILD_DIR); make clean)

install:
	@echo "install not supported\n"

##### Any additional, platform-specific rules come here:

